import ../../localstd/io;

# import enum_error1;
# import enum_error2;
# import enum_error3;

fnc entry main() : i32 {
	
	enum {
		size = 10
	}
	
	# test for defining the size*2 as const expr
	printf("size*2 %d\n", size*2);
	let a: i32[size];
	a[size-1] = 1;
	printf("a[size-1] : %d\n", a[size-1]);

	return 0;
}