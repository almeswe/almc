from ../localstd/io import printf;

fnc entry main() : void {
	let a: i32[10];
	let size: i32 = lengthof a / lengthof *a;

	for (let i: i32 = 0; i < size; i += 1)
		printf("%d) %d\n", i+1, a[i] = i);

	printf("additional: %d", *a);
}