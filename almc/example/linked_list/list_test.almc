import list;

from ../localstd/io import scanf;

fnc entry main() : i32 {
	let l: linked_list* = linked_list_new();

	for (let i: i32 = 1; i <= 10; i += 1) {
		linked_list_add(l, i);
	}

	print_linked_list(l);
	for (;;);
	return 0;
}
